# DeGift 功能架构文档

## 📋 概述

DeGift 是一个去中心化礼物系统，允许用户通过智能合约发送和接收代币或 NFT 作为礼物。

## 🏗️ 项目结构

### 前端组件 (`apps/web/src/`)

```
apps/web/src/
├── app/
│   ├── gift/
│   │   ├── create/
│   │   │   └── page.tsx              # 礼物创建页面
│   │   └── [id]/
│   │       └── page.tsx              # 礼物详情页面
│   └── gifts/
│       └── page.tsx                  # 礼物列表页面
│
├── components/
│   └── gift/
│       ├── CreateGiftForm.tsx        # 礼物创建表单
│       ├── TokenSelector.tsx         # 代币选择器
│       ├── NFTSelector.tsx           # NFT 选择器
│       ├── GiftThemeSelector.tsx     # 礼物主题选择器
│       ├── GiftCard.tsx              # 礼物卡片组件
│       ├── GiftList.tsx              # 礼物列表组件
│       └── ClaimGift.tsx             # 领取礼物组件
│
└── lib/
    └── gift-types.ts                 # 类型定义
```

## 🎯 核心功能

### 1. 礼物创建 (ZES-77)

**路由**: `/gift/create`

**功能特性**:
- ✅ 礼物类型选择（TOKEN / NFT）
- ✅ 代币选择器（支持 ETH、USDC、DAI 及自定义代币）
- ✅ NFT 选择器（显示用户持有的 NFT）
- ✅ 接收者地址输入
- ✅ 礼物祝福消息编辑
- ✅ 主题模板选择（6 种预设主题）
- ✅ 有效期设置（1/3/7/14 天）
- ✅ 钱包连接状态检查

**核心组件**:
- `CreateGiftForm` - 主表单组件
- `TokenSelector` - 代币选择和金额输入
- `NFTSelector` - NFT 选择
- `GiftThemeSelector` - 主题选择

### 2. 礼物展示和领取 (ZES-78)

**路由**:
- `/gift/[id]` - 礼物详情页
- `/gifts` - 礼物列表页

**功能特性**:
- ✅ 礼物卡片设计（展示礼物信息）
- ✅ 礼物状态显示（Active / Claimed / Expired）
- ✅ 领取权限验证
- ✅ 领取按钮和交互
- ✅ 礼物列表（发送的/接收的）
- ✅ 筛选和分类
- ✅ 交易哈希显示

**核心组件**:
- `GiftCard` - 礼物卡片展示
- `GiftList` - 礼物列表和筛选
- `ClaimGift` - 领取流程处理

### 3. 移动端适配 (ZES-80)

**功能特性**:
- ✅ 响应式布局（使用 Tailwind 断点）
- ✅ 触摸优化（`touch-manipulation`）
- ✅ 移动端字体大小适配（xs: 和默认尺寸）
- ✅ 玻璃态 UI（glass-card / glass-button 样式）

## 📦 数据类型

### Gift 接口
```typescript
interface Gift {
  id: string
  giftId: string
  txHash?: string
  creator: { id: string; address: string }
  giftType: 'TOKEN' | 'NFT'
  token: string
  tokenSymbol?: string
  tokenDecimals?: number
  amount: string
  recipient: string
  message: string
  theme?: string
  status: GiftStatus
  claimed: boolean
  expireTime: string
  createdAt: string
}
```

### 礼物主题
6 种预设主题：
- Classic Red (默认)
- Ocean Blue
- Royal Purple
- Golden Shine
- Lucky Green
- Sweet Pink

## 🎨 设计系统

### 样式类
- `glass-card` - 玻璃态卡片背景
- `glass-button` - 玻璃态主按钮
- `glass-button-secondary` - 玻璃态次要按钮
- `text-text-primary-light` - 主要文本颜色
- `text-text-secondary-light` - 次要文本颜色

### 响应式断点
- `xs:` - 小屏幕优化（475px+）
- `sm:` - 平板（640px+）
- `lg:` - 桌面（1024px+）

## 🔄 待实现功能

### API 集成
- [ ] 连接后端 API 获取礼物列表
- [ ] 实现礼物创建的链上交互
- [ ] 实现礼物领取的链上交互
- [ ] NFT 元数据获取

### 链上交互
- [ ] 智能合约集成（使用 wagmi/viem）
- [ ] 代币授权（Approve）处理
- [ ] Gas 费用估算
- [ ] 交易状态追踪

### 用户体验
- [ ] 礼物开启动画
- [ ] NFT 3D 预览
- [ ] 分享功能（生成分享链接）
- [ ] 二维码生成
- [ ] 推送通知

### 性能优化
- [ ] 图片懒加载
- [ ] 代码分割
- [ ] 首屏加载优化
- [ ] PWA 支持（可选）

## 🚀 下一步计划

1. **Phase 1**: 完成前端基础功能 ✅
   - [x] 创建所有页面和组件
   - [x] 实现基本 UI 和交互
   - [x] 响应式布局

2. **Phase 2**: API 和链上集成
   - [ ] 连接后端 API
   - [ ] 智能合约集成
   - [ ] 钱包交互完善

3. **Phase 3**: 用户体验优化
   - [ ] 动画效果
   - [ ] 分享功能
   - [ ] 性能优化

4. **Phase 4**: 移动端深度优化
   - [ ] PWA 支持
   - [ ] 触摸手势
   - [ ] 离线缓存

## 📝 技术栈

- **框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **Web3**: wagmi, viem, RainbowKit
- **状态管理**: React Hooks, Zustand
- **UI 组件**: 自定义组件 + Material Symbols Icons

## 📚 相关文档

- [Linear ZES-77: 礼物创建界面开发](https://linear.app/zesty-studio/issue/ZES-77)
- [Linear ZES-78: 礼物展示和领取页面](https://linear.app/zesty-studio/issue/ZES-78)
- [Linear ZES-80: 移动端适配和优化](https://linear.app/zesty-studio/issue/ZES-80)

## 👥 负责人

- **前端开发**: Ruolynn Chen
- **任务创建时间**: 2025-11-06
